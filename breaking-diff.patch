--- Current vercel.json (BROKEN)
+++ Fixed vercel.json (WORKING)
@@ -1,30 +1,22 @@
 {
   "version": 2,
   "builds": [
     {
       "src": "package.json",
       "use": "@vercel/static-build",
       "config": {
-        "buildCommand": "npm run build",
+        "buildCommand": "vite build",
         "outputDirectory": "dist/public"
       }
     },
-    {
-      "src": "api/**/*.js",
-      "use": "@vercel/node"
-    }
+  ],
+  "functions": {
+    "dist/index.js": {
+      "runtime": "nodejs18.x"
+    }
   ],
   "routes": [
     {
       "src": "/api/(.*)",
-      "dest": "/api/$1.js"
+      "dest": "/dist/index.js"
     },
     {
       "handle": "filesystem"
     },
     {
       "src": "/(.*)",
       "dest": "/index.html"
     }
   ]
 }

KEY CHANGES:
1. Removed @vercel/node build for api/**/*.js (these files don't exist)
2. Added functions config pointing to your actual Express server at dist/index.js
3. Updated API routes to point to your Express server instead of non-existent api files
4. Simplified build command to just vite build (server builds separately via npm run build)

ROOT CAUSE: Your Express app architecture doesn't match Vercel's expected serverless function structure.